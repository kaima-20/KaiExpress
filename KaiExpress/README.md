# KaiExpress

Minimal scaffold for the KaiExpress food delivery site.

Quickstart:

1. Create a virtualenv and install deps:

```bash
python -m venv venv
venv\Scripts\activate    # Windows
pip install -r requirements.txt
```

2. Set config in `.env` and run:

```bash
python app.py
```
